import { Button, Input, Link } from "@jamsr-ui/react";

const Page = () => {
  return (
    <div>
      <Link>HI</Link>
      <Button>Hii</Button>
      <Input label="Hello" />
    </div>
  );
};

export default Page;
