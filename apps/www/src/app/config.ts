export type ImageMetadata = {
  name: string;
  url: string;
  placeholder: string;
  width: number;
  height: number;
};

export type FileMetadata = {
  name: string;
  url: string;
  size: number;
};
